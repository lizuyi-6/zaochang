# This host has 1.6 GiB RAM and a 4 GiB swapfile. A low non-zero value lets the
# single-concurrency ClamAV process page cold application memory instead of
# being killed while keeping anonymous browsing responsive.
vm.swappiness = 20
