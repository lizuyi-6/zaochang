# 造场前端最终验收报告

日期：2026-09-18 · 验收人：ZCode（前端终验角色） · 环境：本地 dev(vinext + Wrangler 本地 D1/R2，APP_ENV=development)

## 1. 结论(Verdict)

**PASS WITH KNOWN ISSUES**

发现的 P0/P1 级前端问题已全部修复并通过浏览器实测回归；自动化门控全绿。剩余最重要的事项不是缺陷而是功能缺口：**已发布作品没有"编辑→重新送审"入口(Journey D 不可执行)**,需要产品决策是否排期。在此缺口被决策之前，不建议做大规模拉新，但可以按计划进入邀请制公测。

## 2. 验收范围(Scope)

- 页面地图：32 条前端路由 + 50 条 API 路由；本轮实测覆盖 /, /discover, /feed, /circles, /challenges, /collections, /bookshelf(列表/书/章节), /product/[slug](种子+数据库作品), /wallet, /notifications, /studio, /studio/new, /profile, /developers, /developers/docs, /docs, /guide, /app, /galaxy, /founder, /admin, /signin, /oauth/authorize, /lattice/(壳), 以及不存在的随机路径(404)。
- 角色：匿名、普通成员(finalcheck)、创作者(preview)、管理员(admin)、创始人(founder)。
- 视口：1920×1080、1440×900、1366×768、1024×768、820×1180、430×932、390×844、375×812、320×568。
- 数据：全部为本地 dev D1 中真实账号/真实接口产生的数据；为测试支付成功路径，按双入口账本规范向测试买家钱包注入 20 果(fruit_operations + fruit_entries + wallets + transactions 四表一致，通过钱包完整性校验),验收结束后该笔测试注资与订单流水仍保留在本地 dev 库中，不影响生产。

## 3. 已修复问题(本轮直接修复并回归验证)

| ID | 级别 | 区域 | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|---|---|
| F-01 | P1 | 首页 hero | "带一件作品来" CTA 在 1440px 被压成 12px 宽(逐字竖排),320px 下完全不可见 | `.text-action` 是 flex 子项但无 `white-space:nowrap/flex-shrink:0`，被兄弟按钮挤压 | `.text-action` 加 nowrap+flex-shrink:0;`.home-stage-actions` 加 flex-wrap+row-gap | 320/1440/1920 实测三 CTA 完整可读，无横向溢出 |
| F-02 | P1 | 收藏页 | "最近收藏"展示 4 件用户从未收藏的静态种子作品(假内容);真实收藏的数据库作品在收藏夹里不可见(计数 01 但显示"还是空的") | 客户端只把收藏引用解析到静态种子表，数据库作品 id 永远解析不到 | 合并 /api/community 下发的已发布作品(hydrateProductRow)+种子表统一解析;"最近收藏"改为展示真实收藏，空态诚实化；示例架标注"灵感示例"且不再可选中 | 实测收藏"深夜电台模拟器"后出现在"最近收藏"与"稍后体验"夹内，链接正确 |
| F-03 | P1 | 发布向导 | 第 1 步点"继续"在校验不通过时无任何反馈(静默失败) | next() 直接 return,无错误态 | 增加 stepError:字段级具体文案("作品名称至少需要 2 个字"/"一句话介绍至少需要 12 个字"),role=alert，输入即清除 | 实测两步错误文案分别出现并可继续 |
| F-04 | P1 | 全局 404 | 不存在路径返回裸白页"Not Found"纯文本，无品牌、无归路 | 无 app/not-found.tsx | 新增样式化 not-found.tsx(站壳内)+ 全局 error.tsx(含重试);删除站壳里失效的 /signout 豁免使 404 带完整导航 | 桌面/390px 截图确认，含"回到首页/去探索作品" |
| F-05 | P2 | ⌘K 搜索 | 命令面板只搜 6 件静态种子作品，搜不到任何社区真实作品 | results 仅过滤静态 products | 面板首次打开时拉取 /api/community 公开作品并合并；修复数据库作品链接 /product/undefined → /product/[id] | 实测搜索"深夜电台"命中并跳转 /product/1 |
| F-06 | P2 | 书架目录 | 章节页左侧目录出现横向滚动条 | `.book-toc-list` grid 隐式列按 max-content 撑开(242px > 容器 213px) | `grid-template-columns: minmax(0, 1fr)`，长标题按既有设计省略号截断 | 实测 scrollW=clientW,溢出消除 |
| F-07 | P2 | 侧栏 | "七月造物挑战"文案过期(当前 9 月，挑战实为开放命题) | 硬编码月份 | 改为"造物挑战"(与挑战页口径一致) | 截图确认 |
| F-08 | P3 | 动态页 | 喜欢/评论图标按钮可访问名只有裸数字("0") | 无 aria-label | 加 `aria-label="喜欢,当前 N 次"`/"评论,当前 N 条" | 快照确认 |
| F-09 | P3 | 主页/个人页 | 个人页大头像首字母不大写，与站壳头像不一致 | 两处取值逻辑不同 | 统一用 member.initial(大写) | 代码审查 |
| F-10 | P3 | 全局 a11y | 无"跳到主要内容"链接 | 未实现 | 站壳加 .skip-link + main#main-content(tabIndex=-1),聚焦可见 | DOM 验证存在性；:focus 与 :focus-visible 双写 |
| F-11 | P1 | 构建 | 工作区 tsc 失败：prompts.ts 引用未定义类型 CourseLecture/CourseSession(此前会话遗留) | 类型注解引用了从未定义的名字 | 从 CourseUnit 内联形状提取命名类型 CourseSession/CourseLecture 并复用 | tsc 0 错误，340/340 测试通过 |

## 4. 遗留问题(本轮未修，附原因)

| ID | 级别 | 区域 | 问题 | 状态与建议 |
|---|---|---|---|---|
| R-01 | **P1 功能缺口** | 创作台 | **已发布作品无编辑入口**:无编辑 UI、无编辑 API(/api/products 仅 POST);`review_version` 再审核机制在 schema 层存在但无触发路径。Journey D 不可执行 | 不属于"前端缺陷"而是功能未建；涉及审核安全规则，按任务约束不在验收期临时实现。建议产品排期 |
| R-02 | P3 | 创作台 | 数据图表在作品全为 0 时展示固定高度的占位柱，容易被误读为真实数据 | 建议零数据时显式标注"还没有数据" |
| R-03 | P3 | 开发者中心 | 注册表单仅浏览器原生校验气泡，无字段旁自定义错误 | 可接受；建议后续统一 inline 错误 |
| R-04 | P3 | 动态页 | 喜欢/评论按钮点击高度约 13px(桌面),低于 24px 建议触控目标 | 建议增加垂直 padding |
| R-05 | 观察项 | 动态页 | IAB/Playwright 自动化点击在该页反复超时(其他页面正常);实测无遮挡、无动画抖动、JS 派发点击正常工作，判断为自动化 harness 与 `scroll-behavior:smooth` 的actionability 时序问题，非用户可感缺陷 | 记录为测试环境备注 |
| R-06 | P3 | 种子内容 | 6 件预置作品封面依赖 Unsplash 外链 | 外部可用性依赖；建议迁移至自有 R2 |
| R-07 | 说明 | 安全 | npm audit:2 个 moderate(sanitize-html),0 high | 与构建链相关，非运行时直接暴露；持续跟踪 |

## 5. 响应式结果

8 个视口 × 6 个核心页面(48 组合)程序化扫描：**全部无文档级横向溢出、无超宽文本元素**。320×568 强制档通过：首页 hero、付费墙、探索、钱包、收藏均可用，移动底部导航与创作 FAB 正常。820×1180 平板落入移动端壳(底部导航),布局无破损。书架章节页 390px 进入沉浸阅读模式(目录抽屉 + 问 AI 悬浮钮)。

## 6. 无障碍结果

- 全局 `:focus-visible` 焦点环存在(3px coral 30%);表单焦点有边框强化。
- 新增 skip link(F-10);修复图标按钮可访问名(F-08)。
- `prefers-reduced-motion` 媒体查询存在并把全局动画压到 0.01ms;站壳动画经 `useReducedMotion` 门控。
- 页面语义结构完整(banner/complementary/main、heading 层级、role=alert 用于错误)。
- 遗留：R-04 触控目标；部分装饰性英文 eyebrow 不参与朗读顺序问题(aria 上无危害)。

## 7. 核心旅程结果

| 旅程 | 结果 | 证据要点 |
|---|---|---|
| A 匿名→作品→登录→回跳 | ✅ | 付费墙对匿名显示"登录后继续"→ /signin?return_to=%2Fproduct%2F1 → 登录后回到 /product/1 |
| B 成员动态/喜欢/评论/收藏 | ✅ | 发帖、点赞 0→1、评论即时出现且计数同步；收藏后与收藏页一致(F-02 修复后) |
| C 创作者创建→预审 | ✅ | 三步向导→成功页明确"审核通过前不会公开/交易";创作台显示"审核中";公开接口查不到 pending 作品 |
| D 编辑已发布作品→再审 | ❌ 不可执行 | 功能不存在(R-01) |
| E 付费购买→刷新→重开→退款 | ✅ | 余额不足内联报错(含"果子不能购买"说明)→注资后购买成功、顶栏余额即时同步 20→15→刷新权限保持→钱包退款(反向分录，原流水保留)→付费墙回归 |
| F 开发者注册→授权 | ✅ | 注册→密钥一次性展示(alert+确认按钮)→授权页逐范围中文解释+未验证应用警告→拒绝后 303 回跳 `error=access_denied&state=...`(PKCE S256 强制，缺失时报错页清晰) |
| G 管理员审核 | ✅ | /admin 控制台(指标+预审队列+举报+风险标签页)批准作品后队列清空；非创始人访问 /founder 返回 404(fail-closed),创始人视图正常且与管理员权限明确分离 |
| H 书架→书→章节→移动阅读 | ✅ | 三本书可见、章节渲染、目录树修复后无横滚；390px 沉浸模式干净 |

## 8. 自动化门控(真实结果)

- `npx tsc --noEmit`:**0 错误**(修复 F-11 后)
- `npm run lint`:**0 error / 7 warning**(全部为 hyperknow 模块历史遗留 unused-vars，非本轮改动文件；本轮触及的 7 个文件单独 lint 0 警告)
- `npm test`:**340/340 通过,0 fail 0 skip**(6 套件，含构建，最终修复后重跑两次均绿)
- `npm run db:generate`:"No schema changes"(无意外 schema 漂移)
- `git diff --check`:干净(仅 CRLF 提示)
- `npm audit --omit=dev --audit-level=high`:**0 high**(2 moderate,R-07)
- 网络健康：首页 performance entries 无 4xx/5xx 资源

## 9. 已知风险

1. **R-01(编辑→再审缺口)**是当前最大的产品一致性风险：创作者无法修正被拒/已过审作品，长期会催生"删了重发"的规避行为。建议优先排期。
2. 果子成功路径的验收使用了本地库直接注资(双入口一致);生产环境无此通道，该路径上线前建议用运营发放工具再验一次。
3. 站内预置作品封面依赖 Unsplash(R-06),弱网/被墙环境会破图。
4. 本轮在 dev 环境验证；Workers 生产边缘行为(缓存、地区延迟)未在本轮覆盖。
5. hyperknow(见界)模块有大量未提交的并行改动(非本轮范围);其 lint 警告建议由对应负责人清理。

## 10. 发布建议

**可以有条件进入邀请制公测**:P0 不存在，P1 缺陷均已修复并回归；支付/审核/OAuth/权限门控等安全相关行为实测符合设计且未发现被削弱。建议：
1. 公测前产品侧明确 R-01(作品编辑与再审)的排期口径，并在创作台对"已发布作品不可编辑"给出明确说明，避免用户困惑；
2. 安排 R-02/R-04 两个体验小修；
3. 将本报告 R 项录入 backlog 后放行。
