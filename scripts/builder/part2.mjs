// 第二部分 · 数字的具象化: 数据的表示与运算 (04 ~ 09)
export const part2Docs = [
  {
    id: "doc:hello-computer-part-2",
    slug: "part-2",
    parentId: "'doc:book-hello-computer'",
    title: "第二部分 · 数字的具象化: 数据的表示与运算",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 3,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: ""
  },
  {
    id: "doc:hello-computer-04-complement",
    slug: "04-complement",
    parentId: "'doc:hello-computer-part-2'",
    title: "04 符号位与补码的圆环几何本质",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 4,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 04 符号位与补码的圆环几何本质

## 1. 一个真实问题

在由晶体管构成的数字电路中，导线上只有两种物理状态：高电平（1）与低电平（0）。
世界上根本不存在一个叫做“负号（$-$）”的物理元件。

为了在计算机中表示负数，工程师最先想到的办法是：**拿出最高位作为符号位，0 表示正，1 表示负，其余各位照常存绝对值**——这就是“原码（Sign-Magnitude）”。

例如在 8 位二进制中：
- $+5 = 00000101_2$
- $-5 = 10000101_2$

这种直觉看起来完美无缺，符合人类从小学到的数学直觉。
但为什么在早期的计算机原型机上，原码很快被完全抛弃？为什么现代所有通用 CPU 的算术逻辑单元内部，都强制使用一种看似反人类的编码——**补码（Two's Complement）**？

---

## 2. 我们最自然的第一反应与撞墙过程

现在，我们假设自己是早期硬件工程师，用原码来设计硬件加法器，尝试计算 $5 + (-5) = 0$：

\`\`\`text
    0000 0101  (+5 的原码)
+   1000 0101  (-5 的原码)
--------------------------
    1000 1010  (结果居然是 -10 的原码！)
\`\`\`

直接相加得到了荒谬的 $-10$！

### 为什么原码在硬件实现上会撞墙？
1. **加法器无法处理符号异号运算**：
   如果用原码做算术，加法器不能直接相加。硬件必须先检查两个数的符号位：
   - 若同号，做加法，符号不变；
   - 若异号，硬件必须先比较两个数的绝对值大小，用大绝对值减小绝对值，符号取大者的符号。
   这意味着 CPU 内部除了加法器，还必须设计一套极其庞大、充满借位延迟的**减法器与数值大小比较器**，硬件成本直接翻倍，时钟频率大幅降低！
2. **存在令人窒息的“双零问题”**：
   在原码中：
   - $+0 = 00000000_2$
   - $-0 = 10000000_2$
   零有两种不同的二进制编码！这意味着软件中每一次判断 \`if (x == 0)\`，硬件都必须拿 \`x\` 分别与 \`00000000\` 和 \`10000000\` 比较两次！

**硬件设计的终极追求：能不能让硬件完全不做减法，只保留一套极简的加法器，却能把正数加负数算得完全正确，同时消灭负零？**

---

## 3. 正确的硬件脑内模型：时钟圆环模型（模运算）

解决这个矛盾的数学本质是**模运算（Modular Arithmetic）**。

想象一个只有 12 个刻度的传统机械时钟（模 $M = 12$）：
假设现在时针指向 10 点，你想把它拨回到 7 点（即计算 $10 - 3$）：

你可以选择两种完全等价的操作：
1. **逆时针往回拨 3 个小时**：$10 - 3 = 7$
2. **顺时针往前拨 9 个小时**：$10 + 9 = 19 \\equiv 7 \\pmod{12}$

\`\`\`mermaid
flowchart TD
    CLOCK_10["当前时针指向 10 点"] -->|逆时针拨 3 个小时 (减法)| CLOCK_7["到达 7 点"]
    CLOCK_10 -->|顺时针拨 9 个小时 (加法)| CLOCK_19["19 点 (模 12 自动溢出圈数)"] --> CLOCK_7
\`\`\`

在这个模 12 的有限系统里：
$$\\text{减 3} \\Longleftrightarrow \\text{加 9}$$
这里的 9，就被称为 $-3$ 针对模 12 的**补数（Complement）**，它们满足 $9 + 3 = 12$。

### 映射到 8 位二进制寄存器：
8 位二进制寄存器最多只能容纳 8 个比特，最高位产生的进位会物理溢出丢失。
这说明 8 位寄存器天然就是一个**模 $M = 2^8 = 256$ 的圆环系统**！

要计算 $A - B$，等价于计算：
$$A - B \\equiv A + (256 - B) \\pmod{256}$$
而 $(256 - B)$ 在二进制硬件里的极速生成方法，恰好就是——**“各位取反，末位加 1”**！

因为：
$$256 - B = (255 - B) + 1 = (11111111_2 - B) + 1$$
$11111111_2 - B$ 对应把 $B$ 的每一位 $0$ 变 $1$、$1$ 变 $0$（纯按位取反），最后加 $1$ 即可完成求补！

---

## 4. 跑一个完整案例：硬件加法器执行 $5 - 5$

我们追踪 8 位补码下计算 $5 + (-5)$ 的真实硬件过程：

1. **$+5$ 的补码**：$[+5]_\\text{补} = 00000101_2$
2. **$-5$ 的补码生成**：
   - 绝对值 5 为 $00000101_2$；
   - 按位取反得到反码：$11111010_2$；
   - 末位加 1 得到补码：$[-5]_\\text{补} = 11111011_2$。
3. **加法器直接求和**：
   \`\`\`text
       0000 0101  ([+5]补)
   +   1111 1011  ([-5]补)
   -------------------------
     1 0000 0000  (最高位进位 1 溢出舍弃, 寄存器只保留 8 位: 00000000)
   \`\`\`
4. **输出结果**：$00000000_2 = 0$！
   运算结果丝毫不差，硬件没有任何条件分支判断，直接使用普通加法器完成了减法！

---

## 5. 为什么 8 位补码能多表示一个 $-128$？

在 8 位有符号数中：
- 原码与反码范围：$-127 \\sim +127$（因为 0 占用了两个编码）；
- 补码范围：**$-128 \\sim +127$**。

这个多出来的 $-128$ 从何而来？
观察 $00000000_2$ 唯一代表了 $0$。
在负数序列中：
- $-1 = 11111111_2$
- $-2 = 11111110_2$
- ...
- $-127 = 10000001_2$
- **$10000000_2$**：在原码中它代表 $-0$，但在补码的圆环模型中，根据模运算的对称性，它自然代表了 $-128$！
  注意：**$-128$ 在 8 位下没有对应的原码和反码**（因为 $+128$ 超出了 8 位正数表示范围）。

---

## 6. 原码、反码、补码、移码全景对比表（408 必背）

设机器字长为 $n$ 位（含 1 位符号位，数值位占 $n-1$ 位）：

| 编码名称 | 正数表示 | 负数表示 | 零的表示 | 定点整数表示范围 (以 8 位为例) | 核心用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **原码** | 符号位为 0，数值位同真值 | 符号位为 1，数值位同真值 | 有 $+0$ ($00000000$) 和 $-0$ ($10000000$) | $-127 \\sim +127$ | 浮点数尾数存储、直观人机交互 |
| **反码** | 同原码 | 符号位为 1，数值位各位取反 | 有 $+0$ ($00000000$) 和 $-0$ ($11111111$) | $-127 \\sim +127$ | 补码计算的中间过渡态 |
| **补码** | 同原码 | 符号位为 1，数值位各位取反加 1 | **唯一零** ($00000000$) | **$-128 \\sim +127$** | **所有现代 CPU 定点运算标准** |
| **移码** | 补码符号位取反 | 补码符号位取反 | 唯一零 ($10000000$) | $-128 \\sim +127$ | **IEEE 754 浮点数阶码**（保持单调递增性） |

---

## 7. 408 对应考点与命题陷阱

- **陷阱 1**：“已知 $[X]_\\text{补} = 10000000$，求真值 $X$。”
  - **解析**：很多考生习惯性做“取反加 1”，得到 $01111111 + 1 = 10000000$，误以为是 0。**对于 8 位补码 $10000000$，这是固定定义的最小值 $-128$**，无需也不能套用常规正负转换。
- **陷阱 2**：“补码加法产生进位一定意味着发生了溢出。”
  - **解析：错误！** 补码减法计算 $5 + (-3) = 00000101 + 11111101 = 1\\ 00000010$（产生进位 1，结果 2 正确）。最高位的进位是模运算圆环自然产生的，**溢出只与数值范围超界有关，与最高位进位无必然等价关系**。

---

## 8. 下一章自然产生的问题

既然补码让加法器接管了一切算术，那么如果两个很大的正数相加（例如 8 位下 $80 + 60 = 140 > 127$），加法器算出的结果必然会侵占符号位变成负数。
硬件如何在运算完成的瞬间，毫秒不差地捕获这种“数值溢出”？CPU 内部的 $OF$ 和 $CF$ 标志位到底有什么本质区别？

下一章：**第 05 章 定点数加减法与溢出判断的三种机制**。
`
  },
  {
    id: "doc:hello-computer-05-fixed-point-add-sub",
    slug: "05-fixed-point-add-sub",
    parentId: "'doc:hello-computer-part-2'",
    title: "05 定点数加减法与溢出判断的三种机制",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 5,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 05 定点数加减法与溢出判断的三种机制

## 1. 一个真实问题

在 C 语言程序中，定义两个 \`signed char\`（8 位有符号整数）：

\`\`\`c
signed char a = 80;
signed char b = 60;
signed char c = a + b;
printf("%d\\n", c); // 打印输出居然是 -116！
\`\`\`

为什么两个正数相加，在计算机里变成了一个大负数？
硬件在执行加法时，如何能在纳秒级别准确判断出“这次计算超出了表示范围，结果是错误的”？

---

## 2. 溢出（Overflow）的物理本质

在有限字长的计算机中，运算结果超出了该字长所能表示的最大范围，称为**溢出**。

以 8 位有符号数为例，表示范围是 $[-128, +127]$：
- $80 + 60 = 140 > 127$（**正溢出 / 上溢**）；
- $-80 + (-70) = -150 < -128$（**负溢出 / 下溢**）。

\`\`\`text
    80 的补码:   0101 0000
+   60 的补码:   0011 1100
---------------------------
    运算结果:    1000 1100  (最高位符号位变成了 1，变成了 -116 的补码！)
\`\`\`

由于最高数值位产生的进位直接“冲垮”了符号位，导致正数相加变成了负数。

---

## 3. 408 核心考点：溢出判断的三种硬件机制

数字逻辑设计中，有三种经典电路判定溢出：

\`\`\`mermaid
flowchart TD
    OVERFLOW_METHODS["408 溢出判断三种硬件机制"]
    OVERFLOW_METHODS --> M1["1. 双符号位法 (模 4 补码 / 变形补码)"]
    OVERFLOW_METHODS --> M2["2. 单符号位进位比较法 (Cs ⊕ Cp)"]
    OVERFLOW_METHODS --> M3["3. 操作数与结果符号比较法"]

    M1 -->|01| POS_OV["正溢出 (最高位0表明本应为正)"]
    M1 -->|10| NEG_OV["负溢出 (最高位1表明本应为负)"]
    M1 -->|00/11| NO_OV["无溢出 (合法结果)"]
\`\`\`

### 机制一：双符号位法（模 4 补码 / 变形补码）——大题最常考
- 在 ALU 内部，每个数分配 **2 个符号位**：\`00\` 表示正数，\`11\` 表示负数。
- **运算判定规则**：
  - \`00\`：结果为正，无溢出；
  - \`11\`：结果为负，无溢出；
  - **\`01\`**：最高符号位为 0（本应为正），第二符号位为 1（进位侵占）-> **正溢出（上溢）**；
  - **\`10\`**：最高符号位为 1（本应为负），第二符号位为 0（借位破坏）-> **负溢出（下溢）**；
  - **关键结论**：**最高符号位永远代表真实的理论符号！**

### 机制二：单符号位进位比较法
设 $C_s$ 为符号位产生的进位（向更高位的进位），$C_p$ 为最高数值位向符号位的进位：
$$\\text{溢出标志 } OF = C_s \\oplus C_p$$
- 若 $C_p=1, C_s=0$（数值位进位但符号位没进位）：$OF = 1 \\oplus 0 = 1$（正溢出）；
- 若 $C_p=0, C_s=1$（数值位没进位但符号位进位）：$OF = 0 \\oplus 1 = 1$（负溢出）；
- 硬件只需一个**异或门（XOR）**即可瞬间输出 $OF$ 信号。

### 机制三：$OF$ 标志与 $CF$ 标志的本质区别（408 必考死穴）

| 标志位名称 | 全称与功能 | 适用数据类型 | 判定规则 |
| :--- | :--- | :--- | :--- |
| **$OF$ 标志** | **Overflow Flag (溢出标志)** | **专门用于有符号数运算！** | $OF=1$ 表示有符号运算超出了有符号数范围，结果不可信。 |
| **$CF$ 标志** | **Carry Flag (进位/借位标志)** | **专门用于无符号数运算！** | 加法时 $CF=\\text{最高位进位}$；减法时 $CF=\\text{借位}$。$CF=1$ 表示无符号数产生进位或借位。 |

---

## 4. 408 对应考点与命题陷阱

- **陷阱 1**：“无符号数加法如果 $OF=1$，说明结果错误。”
  - **解析：错误！** 无符号数运算只看 $CF$ 标志，根本不关心 $OF$ 标志。即使 $OF=1$，只要 $CF=0$，无符号数加法结果就是完全正确的。
- **陷阱 2**：“$A - B$ 产生溢出的充分必要条件是两个操作数符号相同。”
  - **解析：错误！** 两个正数相减（如 $80 - 60 = 20$）绝对不可能溢出；只有**异号相减**（如正数减负数：$80 - (-60) = 140$）或者**同号相加**才可能发生溢出。

---

## 5. 下一章自然产生的问题

加减法问题已经彻底解决。但是，如果两个 32 位整数相乘（乘法），或者相除（除法），加法器该如何配合移位操作完成计算？
为什么著名的 **Booth 一位乘法** 能够大幅减少加法次数？加减交替除法又是如何做到不恢复余数的？

下一章：**第 06 章 定点数乘除法: Booth 一位乘与加减交替除法**。
`
  },
  {
    id: "doc:hello-computer-06-mult-div",
    slug: "06-mult-div",
    parentId: "'doc:hello-computer-part-2'",
    title: "06 定点数乘除法: Booth 一位乘与加减交替除法",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 6,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 06 定点数乘除法: Booth 一位乘与加减交替除法

## 1. 一个真实问题

加法器只需要 1 个时钟周期就能算出两数之和。
但在没有专用硬件乘法器的微架构中，乘法 $A \\times B$ 相当于多项累加。例如 $13 \\times 11$：

\`\`\`text
       1 1 0 1  (13)
    x  1 0 1 1  (11)
    ------------
       1 1 0 1  (乘数最低位为1, 加被乘数)
     1 1 0 1    (乘数第二位为1, 左移后加被乘数)
   0 0 0 0      (乘数第三位为0, 只移位不加)
 1 1 0 1        (乘数最高位为1, 左移后加被乘数)
----------------
 1 0 0 0 1 1 1 1  (143)
\`\`\`

手工计算需要根据每一位是 0 还是 1 来决定“加还是不加”。
硬件如何将这一过程变成由寄存器自动执行的流水节拍？对于带有负号的补码，著名的 **Booth 一位乘法** 是如何做到无需转换符号、直接进行有符号乘法运算的？

---

## 2. Booth 一位乘法的核心直觉

假设乘数有一串连续的 1，例如二进制的 \`01111100\`（数值为 $2^6+2^5+2^4+2^3+2^2 = 124$）。
如果按常规算法，需要进行 5 次乘加操作。

但数学上：
$$01111100_2 = 10000000_2 - 00000100_2 = 2^7 - 2^2$$
只需要**在连续 1 的起始处减一次被乘数，在连续 1 的结束处加一次被乘数**，中间全部只移位不加！

**Booth 一位乘法就是这个数学思想的硬件自动化实现**：通过在乘数末尾增加一个辅助位 $y_{n+1}$（初值为 0），通过检查相邻两位 $(y_n, y_{n+1})$ 的变化，自动检测连续 1 的起始和结束。

---

## 3. Booth 乘法硬件规则表（408 必背）

\`\`\`mermaid
flowchart TD
    BITS["观察乘数最低两位 (y_n, y_{n+1})"]
    BITS -->|0 0 或 1 1| PASS["无加减操作, 部分积与乘数一起【算术右移 1 位】"]
    BITS -->|0 1 (由0变1, 连续1开始)| ADD["部分积 + [X]_补, 然后【算术右移 1 位】"]
    BITS -->|1 0 (由1变0, 连续1结束)| SUB["部分积 - [X]_补 (即 + [-X]_补), 然后【算术右移 1 位】"]
\`\`\`

### 运算步骤与约束（大题解题口诀）：
1. 被乘数 $[X]_\\text{补}$ 和部分积采用**双符号位**；
2. 乘数 $[Y]_\\text{补}$ 后面补一个辅助位 $y_{n+1} = 0$；
3. 循环 $n$ 次（$n$ 为数值位位数）：根据 $(y_n, y_{n+1})$ 决定加 0、加 $[X]_\\text{补}$ 或加 $[-X]_\\text{补}$，然后**双符号位部分积与乘数连环算术右移 1 位**；
4. **第 $n+1$ 步（最后一步）只根据最后两位做加减，绝对不移位！**

---

## 4. 定点除法：加减交替法（不恢复余数法）

传统原码除法是“试商法”：若余数减去除数后为负，说明不够除，必须“恢复余数”（加上除数）。这种来回试探在硬件上极耗时间。

**加减交替法**利用数学变换消除了恢复余数的过程：
- 若当前余数 $R_i > 0$：商上 1，余数左移 1 位，下一轮减去除数（即 $2R_i - Y$）；
- 若当前余数 $R_i < 0$：商上 0，余数左移 1 位，下一轮**直接加上除数**（即 $2R_i + Y$）。

运算步数严格固定为 $n+1$ 步加减法，彻底消除了硬件时钟不确定性。

---

## 5. 本章小结

- Booth 乘法直接支持有符号补码乘法，依靠辅助位 $(y_n, y_{n+1})$ 实现加减移位；
- 加减交替除法消除了恢复余数的硬件时延。
`
  },
  {
    id: "doc:hello-computer-07-ieee754",
    slug: "07-ieee754",
    parentId: "'doc:hello-computer-part-2'",
    title: "07 浮点数与 IEEE 754 标准全解",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 7,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 07 浮点数与 IEEE 754 标准全解

## 1. 一个真实问题

在定点数中，小数点的位置是固定的。
如果用 32 位定点整数，最大只能表示约 $2 \times 10^9$；
但在科学计算中，物理学家既要表示宇宙中原子的数量（$10^{80}$），又要表示普朗克常数（$6.626 \times 10^{-34}$）。

为了在固定 32 位长度下容纳跨越一百多个数量级的超大数与超小数，计算机采用了**科学计数法**的硬件实现——**浮点数（Floating-Point Number）**。

1985 年颁布的 **IEEE 754 标准** 统一了全球所有处理器的浮点数格式。它究竟是如何精巧地把符号、阶码和有效数字打包进 32 个比特中的？

---

## 2. IEEE 754 单精度 32 位格式布局

IEEE 754 单精度（\`float\`）格式严格分配如下：

\`\`\`
 1 位     8 位                  23 位
+---+----------+---------------------------------------+
| S |    E     |                   M                   |
+---+----------+---------------------------------------+
 符号   阶码(移码)                尾数(有效数字小数部分)
\`\`\`

- **S（Sign，符号位，1 位）**：\`0\` 表示正数，\`1\` 表示负数。
- **E（Exponent，阶码，8 位）**：使用移码表示，偏置常数 **$\\text{Bias} = 127$**（即 $2^{8-1}-1$）。
  真实指数 $e = E - 127$。
- **M（Fraction / Mantissa，尾数，23 位）**：对于常规数字，二进制科学计数法形式恒为 $1.M_2$。**最高位的 \`1.\` 永远省略不存（隐含位）**，从而免费赚到了 1 位精度（实际具有 24 位有效精度）。

---

## 3. 四大数值区间（408 核心考点与状态机）

IEEE 754 标准根据阶码 $E$ 的取值，将整个 32 位状态空间精巧地划分为四类：

\`\`\`mermaid
flowchart TD
    E_FIELD["阶码字段 E (8 位, 取值 0 ~ 255)"]
    
    E_FIELD -->|E = 1 ~ 254| NORMAL["1. 规格化数 (Normal Numbers)<br/>真值 = (-1)^S * (1.M) * 2^(E - 127)<br/>最高位隐含 1"]
    
    E_FIELD -->|E = 0 且 M = 0| ZERO["2. 零值 (Zero)<br/>+0.0 (S=0) 与 -0.0 (S=1)"]
    
    E_FIELD -->|E = 0 且 M ≠ 0| SUBNORMAL["3. 非规格化数 (Subnormal)<br/>真值 = (-1)^S * (0.M) * 2^(-126)<br/>最高位隐含 0, 解决【渐进下溢】"]
    
    E_FIELD -->|E = 255 且 M = 0| INF["4. 正负无穷大 (±Infinity)<br/>S=0 为 +Inf, S=1 为 -Inf (如 1.0 / 0.0)"]
    
    E_FIELD -->|E = 255 且 M ≠ 0| NAN["5. NaN (Not a Number)<br/>非法运算结果 (如 0.0 / 0.0, 根号 -1)"]
\`\`\`

---

## 4. 跑一个完整转换案例：把 $12.5$ 转为 IEEE 754 单精度二进制

1. **写出十进制真值的二进制形式**：
   $$12.5 = 8 + 4 + 0.5 = 1100.1_2$$
2. **规格化为科学计数法**：
   $$1100.1_2 = 1.1001_2 \times 2^3$$
3. **提取三要素**：
   - **符号位 S**：正数 $\to S = 0$；
   - **真实指数 $e$**：$e = 3$；
   - **阶码 E**：$E = e + 127 = 3 + 127 = 130 = 10000010_2$；
   - **尾数 M**：去掉隐含的 \`1.\`，剩下 \`1001\`，后补 19 个 0 凑满 23 位：
     $$M = 10010000000000000000000_2$$
4. **最终 32 位机器码**：
   \`\`\`text
   0 | 10000010 | 10010000000000000000000
   十六进制表示: 0x41480000
   \`\`\`

---

## 5. 408 对应考点与命题陷阱

- **陷阱 1**：“IEEE 754 单精度非规格化数的真实指数是 $0 - 127 = -127$。”
  - **解析：错误！** 标准规定非规格化数的指数**固定为 $-126$**（与最小规格化数指数相同），同时尾数隐含位变为 $0.M$，这样可以平滑衔接最小正规格化数与零，避免精度突变悬崖（渐进下溢）。
- **陷阱 2**：“$int$ 转 $float$ 会不会损失精度？$float$ 转 $double$ 会不会损失精度？”
  - **解析**：
    - 32 位 $int$ 有 31 位数值位，而 $float$ 只有 24 位有效精度，因此 **$int \to float$ 可能损失低位精度**；
    - $float$（24位有效数字）转 $double$（53位有效数字）**绝对无损精准转换**。

---

## 6. 下一章自然产生的问题

了解了浮点数的表示之后，两个浮点数该如何做加减法？
例如 $1.0 \times 2^3 + 1.1 \times 2^1$，由于指数不同，尾数的小数点并没有对齐，硬件如何自动完成“对阶、求和、规格化与舍入”？

下一章：**第 08 章 浮点数加减运算: 对阶、求和与舍入**。
`
  },
  {
    id: "doc:hello-computer-08-float-arithmetic",
    slug: "08-float-arithmetic",
    parentId: "'doc:hello-computer-part-2'",
    title: "08 浮点数加减运算: 对阶、求和与舍入",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 8,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 08 浮点数加减运算: 对阶、求和与舍入

## 1. 浮点数加减运算 5 步全景流程

\`\`\`mermaid
flowchart TD
    S1["步骤 1: 对阶 (Alignment)<br/>计算阶差, 小阶向大阶看齐, 尾数右移"] --> S2["步骤 2: 尾数加减 (Addition/Subtraction)<br/>补回隐含最高位 1, 定点加减求和"]
    S2 --> S3["步骤 3: 规格化 (Normalization)<br/>左规 (阶码减小) / 右规 (阶码增大)"]
    S3 --> S4["步骤 4: 舍入处理 (Rounding)<br/>0舍1入法 / 恒置1法"]
    S4 --> S5["步骤 5: 溢出判断 (Overflow Check)<br/>阶码大于 254 (上溢) / 阶码小于 0 (下溢)"]
\`\`\`

---

## 2. 五大步骤深度拆解与硬件法则

### 步骤一：对阶（小阶向大阶看齐）——408 大题第一步
- 计算阶差 $\\Delta E = E_A - E_B$；
- **原则：小阶向大阶看齐**。将较小阶码的数其阶码增加到较大者的阶码，同时将其尾数**逻辑右移 $\\Delta E$ 位**。
- **为什么不能“大阶向小阶看齐”？**
  如果大阶向小阶对齐，需要将大阶的尾数左移，这会导致最高位的有效数字被移出丢弃，造成无法挽回的巨大数值误差；而尾数右移只会丢失末尾的微小精度。

### 步骤二：尾数加减求和
- 将隐含位补齐为 $1.M$，转换为双符号位补码后，送入 ALU 执行定点加减。

### 步骤三：规格化（左规与右规）
- **右规（只需 1 次）**：若尾数相加溢出（双符号位出现 \`01\` 或 \`10\`），说明结果 $\ge 2.0$。尾数**右移 1 位**，阶码**加 1**。
- **左规（可能多次）**：若尾数相减导致最高有效位为 0（如 $0.001..._2$），尾数**连续左移**，阶码**相应连续减 1**，直到最高有效位为 1。

### 步骤四：舍入处理
尾数右移丢弃的低位可能引起精度下降：
- **0 舍 1 入法**：移出的最高位如果是 1，在尾数末位加 1（类似四舍五入，可能再次引起右规）；
- **恒置 1 法**：只要移出位中含有 1，直接将尾数最低有效位置 1。

### 步骤五：溢出判定
- **上溢（Overflow）**：阶码经过调整后大于 254（$E > 254$），硬件触发浮点上溢异常；
- **下溢（Underflow）**：阶码经过调整后小于 0（$E < 0$），机器按 0.0 处理。

---

## 3. 本章小结

- 对阶永远是小阶向大阶看齐；
- 尾数相加溢出触发右规（阶码加1），尾数抵消过小触发左规（阶码减1）。
`
  },
  {
    id: "doc:hello-computer-09-alu-cla",
    slug: "09-alu-cla",
    parentId: "'doc:hello-computer-part-2'",
    title: "09 运算器设计: 从一位全加器到超前进位加法器 CLA",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 9,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 09 运算器设计: 从一位全加器到超前进位加法器 CLA

## 1. 行波进位加法器（RCA）的时延死穴

一位全加器（FA）由门电路构成：
- 本位和：$S_i = A_i \\oplus B_i \\oplus C_{i-1}$
- 本位向高位进位：$C_i = A_i B_i + (A_i \\oplus B_i) C_{i-1}$

如果将 32 个一位全加器串联成一个 32 位加法器：
第 31 位的进位必须等待第 0 位依次往上传递 31 次！
每个全加器进位产生需要 2 个门延迟，32 位总共需要 $32 \\times 2 = 64$ 个门延迟。这使得 CPU 无法工作在高主频下。

---

## 2. 超前进位加法器（CLA）的并行数学展开

CLA 的核心思想：**不要等低位算完再传进位，而是利用输入端 $A$ 和 $B$ 的所有信号，通过纯组合逻辑树一次性并行生成所有进位！**

定义两项中间变量：
- **进位生成项 $G_i = A_i \\cdot B_i$**（只要本位全为 1，不管低位如何，必然独立产生进位）；
- **进位传递项 $P_i = A_i \\oplus B_i$**（只要本位有 1，就能把低位的进位向高位传递）。

则任意位的进位可以直接展开：
$$C_0 = G_0 + P_0 C_{-1}$$
$$C_1 = G_1 + P_1 G_0 + P_1 P_0 C_{-1}$$
$$C_2 = G_2 + P_2 G_1 + P_2 P_1 G_0 + P_2 P_1 P_0 C_{-1}$$
$$C_3 = G_3 + P_3 G_2 + P_3 P_2 G_1 + P_3 P_2 P_1 G_0 + P_3 P_2 P_1 P_0 C_{-1}$$

\`\`\`mermaid
flowchart TD
    INPUT["输入 A_0~A_3, B_0~B_3, C_-1"] --> P_G_GEN["并行生成 P_i 和 G_i (1 级门延迟)"]
    P_G_GEN --> CLA_LOGIC["CLA 与或逻辑树 (2 级门延迟并行产生 C_0, C_1, C_2, C_3)"]
    CLA_LOGIC --> SUM_OUT["并行生成 S_0, S_1, S_2, S_3"]
\`\`\`

无论加法器扩展到多少位，所有进位都在固定 2 个门延迟内同时产生！

---

## 3. 本章小结

- 行波进位是串行传递，延迟随位数线性增加 $O(n)$；
- 超前进位用空间换时间，进位并行产生，延迟降低为常数级 $O(1)$。
`
  }
];
