// 第三部分 · 记忆的金字塔: 存储系统基础与主存 (10 ~ 13)
export const part3Docs = [
  {
    id: "doc:hello-computer-part-3",
    slug: "part-3",
    parentId: "'doc:book-hello-computer'",
    title: "第三部分 · 记忆的金字塔: 存储系统基础与主存",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 4,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: ""
  },
  {
    id: "doc:hello-computer-10-memory-pyramid",
    slug: "10-memory-pyramid",
    parentId: "'doc:hello-computer-part-3'",
    title: "10 存储体系金字塔与局部性原理",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 10,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 10 存储体系金字塔与局部性原理

## 1. 一个真实问题

现代 CPU 核心的运算速度极其惊人：在 3.0 GHz 主频下，一个时钟周期只有约 **0.33 纳秒**。
然而，主板上的内存（DRAM）访问一次通常需要 **50 ~ 100 纳秒**。

这意味着：CPU 发出一个读内存请求，必须原地等待 **150 ~ 300 个时钟周期**。
如果 CPU 每执行一步计算都要直接访问主存，那么处理器 99% 的时间都在发呆等待内存数据。

计算机工程师是如何用一套看似不平衡的硬件组合，让整个系统的平均存取速度逼近 1 纳秒的？

---

## 2. 存储层次金字塔模型

理想的存储器应当具有三个属性：**容量无限大、速度无限快、价格无限便宜**。
在物理半导体物理规律的制约下，这三者是不可调和的矛盾：
- 速度极快的存储元件（如触发器构成的寄存器与 SRAM），每比特需要 6 个晶体管，发热巨大、成本昂贵、集成度低；
- 便宜且集成度高的存储元件（如电容构成的 DRAM），充放电与寻址延迟大。

工程师给出的工程解法是：**构建多级存储金字塔**。

\`\`\`mermaid
flowchart TD
    REG["寄存器 (Registers)<br/>延迟 < 0.5 ns, 容量几百字节, 最贵"]
    L1["L1 高速缓存 (L1 Cache)<br/>延迟 ~1 ns, 容量 32KB ~ 64KB"]
    L2["L2 高速缓存 (L2 Cache)<br/>延迟 ~3-5 ns, 容量 256KB ~ 2MB"]
    L3["L3 高速缓存 (L3 Cache)<br/>延迟 ~10-20 ns, 容量 16MB ~ 96MB (多核共享)"]
    DRAM["主存储器 (Main Memory DRAM)<br/>延迟 ~50-100 ns, 容量 16GB ~ 128GB"]
    SSD["外部存储 (SSD / NVMe Flash)<br/>延迟 ~10-50 us, 容量 1TB ~ 4TB"]
    HDD["机械硬盘 / 云存储<br/>延迟 ~5-10 ms, 容量 10TB+, 最便宜"]

    REG --> L1 --> L2 --> L3 --> DRAM --> SSD --> HDD
\`\`\`

---

## 3. 为什么金字塔能够生效？局部性原理的数学本质

如果程序的内存访问是纯随机分布的，那么这套金字塔结构将毫无用处。
金字塔之所以能发挥巨大威力，完全建立在人类编写程序的内在物理规律——**局部性原理（Principle of Locality）**：

### 1. 时间局部性（Temporal Locality）
- **现象**：如果某个内存单元被访问，那么在不久的将来，它**极有可能被再次访问**。
- **软件根源**：循环结构中的计数变量、频繁调用的公共函数、累加器变量。
- **硬件对策**：将刚刚访问过的数据复制一份保存在最靠近 CPU 的极速 Cache 中，下次访问直接命中。

### 2. 空间局部性（Spatial Locality）
- **现象**：如果某个内存单元被访问，那么在不久的将来，**它物理相邻的内存单元极有可能被访问**。
- **软件根源**：顺序执行的代码指令流、一维数组的顺序遍历、连续存放的结构体成员。
- **硬件对策**：**每次访存不是只抓取单个字节，而是按“数据块（Cache Block / Line，如 64 字节）”成批打包调入高速缓存**。

---

## 4. 跑一个完整案例：二维数组按行遍历 vs 按列遍历的性能天壤之别

在 C 语言中，二维数组在内存中是**按行优先（Row-Major）连续存放**的：

\`\`\`c
#define N 2048
int arr[N][N];

// 方案 A: 按行遍历 (充分利用空间局部性)
for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
        sum += arr[i][j]; // 连续物理地址访问！Cache 命中率高达 95% 以上
    }
}

// 方案 B: 按列遍历 (严重破坏空间局部性)
for (int j = 0; j < N; j++) {
    for (int i = 0; i < N; i++) {
        sum += arr[i][j]; // 每次跳跃 2048*4 字节访问！Cache 命中率暴跌到 0%
    }
}
\`\`\`

在真实机器上实测，两段功能完全相同的代码，方案 A 的执行速度通常是方案 B 的 **5 到 10 倍**！

---

## 5. 本章小结

- 存储体系的核心矛盾是速度、容量与成本的权衡；
- 局部性原理（时间局部性与空间局部性）是高速缓存与分页机制成立的物理基石。
`
  },
  {
    id: "doc:hello-computer-11-sram-dram",
    slug: "11-sram-dram",
    parentId: "'doc:hello-computer-part-3'",
    title: "11 SRAM、DRAM 芯片结构与三种刷新机制",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 11,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 11 SRAM、DRAM 芯片结构与三种刷新机制

## 1. SRAM 与 DRAM 物理存储元对比

| 对比维度 | 静态 RAM (SRAM) | 动态 RAM (DRAM) |
| :--- | :--- | :--- |
| **基本存储元结构** | 双稳态触发器 (6 个晶体管 6T) | 单管 MOS + 极微小储能电容 (1T1C) |
| **信息保存物理机制** | 正反馈电平自锁存 | **电容电荷存储**（存在漏电放电） |
| **是否需要定期刷新** | **不需要刷新**（通电即可保持） | **必须定期动态刷新**（通常每 2ms 需刷新一次） |
| **读出方式** | 非破坏性读出（读后原信息不改变） | **破坏性读出**（读出时电容电荷被放空，必须立即自动重写恢复） |
| **引脚复用技术** | 地址线不复用（地址线引脚多） | **地址引脚分时复用**（行地址和列地址通过同一组引脚先后送入，引脚减半） |
| **集成度与成本** | 集成度低、发热大、造价昂贵 | 集成度极高、发热低、造价极其低廉 |
| **典型应用场景** | CPU 片内 L1 / L2 / L3 高速缓存 | 计算机主内存 (DDR4 / DDR5 SDRAM) |

---

## 2. DRAM 的三种刷新机制（408 大题高频计算）

由于电容存在自然漏电，DRAM 芯片规定所有存储单元必须在**刷新周期（如 $2 \text{ ms}$）**内被全部扫描刷新一遍。

**核心考点：DRAM 刷新是以“存储矩阵的行（Row）”为单位进行的！**
刷新一行数据只需占用一个存取周期 $t_{\text{RC}}$。

假设某 DRAM 芯片存储矩阵共有 **1024 行**，存取周期 $t = 0.5 \ \\\\\mu\text{s}$，刷新周期为 $T_{\text{ref}} = 2 \text{ ms} = 2000 \ \\\\\mu\text{s}$：

\`\`\`mermaid
flowchart TD
    DRAM_REFRESH["DRAM 三种刷新方式"]
    DRAM_REFRESH --> M1["1. 集中刷新: 集中在 2ms 末尾连续刷新 1024 行<br/>死时间 = 1024 * 0.5us = 512us (产生 25.6% 访存死区)"]
    DRAM_REFRESH --> M2["2. 分散刷新: 每个存取周期前半读写 (0.5us)、后半固定刷新 1 行 (0.5us)<br/>无死区, 但主存实际存取周期拉长为 1.0us, 性能减半"]
    DRAM_REFRESH --> M3["3. 异步刷新: 将 1024 次刷新均匀切分在 2ms 期间<br/>刷新间隔 = 2000us / 1024 = 1.95us (每隔 1.95us 仅死区 0.5us, 最优方案)"]
\`\`\`

---

## 3. 408 对应考点与命题陷阱

- **陷阱 1**：“DRAM 刷新是由 CPU 软件发指令执行的。”
  - **解析：错误！** 刷新完全由主板上的 **DRAM 控制器（硬件逻辑）** 定期自主产生行地址完成，对 CPU 和程序员完全透明。
- **陷阱 2**：“容量为 $1024 \times 1024 \times 8$ 位的 DRAM 芯片，外部地址引脚需要 20 根。”
  - **解析：错误！** DRAM 普遍采用**地址引脚复用技术**。内部 $1024 \times 1024$ 矩阵具有 10 位行地址和 10 位列地址，分时复用后，外部物理芯片只需要 **10 根地址引脚**！

---

## 4. 本章小结

- SRAM 靠触发器（快/贵/不刷新），DRAM 靠电容（密/廉/需刷新/引脚复用）；
- 异步刷新是最优工程方案，死时间被均匀打散。
`
  },
  {
    id: "doc:hello-computer-12-mem-expansion",
    slug: "12-mem-expansion",
    parentId: "'doc:hello-computer-part-3'",
    title: "12 主存芯片与 CPU 互连: 位扩展、字扩展与译码器",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 12,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 12 主存芯片与 CPU 互连: 位扩展、字扩展与译码器

## 1. 为什么需要存储器扩展？

单片存储器芯片的物理参数通常有限（如 $16\text{K} \times 8\text{位}$ 或 $64\text{K} \times 1\text{位}$）。
而 CPU 的地址总线可能是 16 位（寻址 64KB），数据总线是 8 位或 32 位。

必须通过芯片级联，将多片小芯片组合成满足 CPU 要求的完整内存模块。

---

## 2. 存储器扩展三大方式

### 1. 位扩展（增加字长）
- **目标**：用多片位宽较窄的芯片拼出完整的数据总线位宽（例如用 8 片 $8\text{K} \times 1\text{位}$ 芯片拼成 $8\text{K} \times 8\text{位}$ 内存）。
- **连线规则**：
  - 所有芯片的**地址线 $A_0 \sim A_{12}$ 全部并联**；
  - 所有芯片的**片选信号 $\overline{CS}$、读写控制线 $\overline{WE}$ 全部并联**；
  - 8 片芯片的数据引脚分别引出，各接数据总线的一根（$D_0 \sim D_7$）。

### 2. 字扩展（增加容量与地址空间）
- **目标**：用多片容量较小的芯片扩大可寻址的单元数量（例如用 4 片 $16\text{K} \times 8\text{位}$ 芯片拼成 $64\text{K} \times 8\text{位}$ 内存）。
- **连线规则**：
  - 低位地址线（$A_0 \sim A_{13}$，14 根线寻址 16K）并联接入所有芯片；
  - 数据线 $D_0 \sim D_7$ 并联接入所有芯片；
  - **高位地址线（$A_{14}, A_{15}$）送入 2-4 译码器（如 74LS138），译码器的 4 个输出引脚分别独立连接 4 片芯片的片选引脚 $\overline{CS}_0 \sim \overline{CS}_3$**。

\`\`\`mermaid
flowchart TD
    CPU_ADDR_HIGH["CPU 高位地址 A15, A14"] --> DECODER["2-4 译码器 (74LS138)"]
    DECODER -->|Y0| CS0["片选 /CS0 -> 芯片 0 (地址范围: 0000H ~ 3FFFH)"]
    DECODER -->|Y1| CS1["片选 /CS1 -> 芯片 1 (地址范围: 4000H ~ 7FFFH)"]
    DECODER -->|Y2| CS2["片选 /CS2 -> 芯片 2 (地址范围: 8000H ~ BFFFH)"]
    DECODER -->|Y3| CS3["片选 /CS3 -> 芯片 3 (地址范围: C000H ~ FFFFH)"]
    
    CPU_ADDR_LOW["CPU 低位地址 A13 ~ A0 (14位)"] ==> CHIPS["并联送达全部 4 片芯片内部行/列译码"]
\`\`\`

---

## 3. 本章小结

- 位扩展并联地址接不同数据线；
- 字扩展并联数据由高位地址译码产生片选 $\overline{CS}$。
`
  },
  {
    id: "doc:hello-computer-13-interleaved-mem",
    slug: "13-interleaved-mem",
    parentId: "'doc:hello-computer-part-3'",
    title: "13 多模块交叉存储器与流水线连续存取",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 13,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 13 多模块交叉存储器与流水线连续存取

## 1. 单模块存储器的速度瓶颈

单个主存 DRAM 模块完成一次读写的存取周期为 $T = 100 \text{ ns}$，而 CPU 内部总线传输一个数据只需 $r = 25 \text{ ns}$。
如果连续读取 4 个连续字，单模块结构总共需要：
$$t_{\text{seq}} = 4 \times 100 \text{ ns} = 400 \text{ ns}$$

如何通过内存编址结构的重新设计，让主存也能像流水线一样并发存取？

---

## 2. 高位交叉编址 vs 低位交叉编址

\`\`\`mermaid
flowchart TD
    ADDR["主存物理地址划分"]
    ADDR -->|高位为模块号, 低位为体内地址| HIGH["高位交叉编址 (顺序存储)<br/>同一个模块存满连续地址空间<br/>连续取指或读数组集中在同一模块 -> 无法并发！"]
    ADDR -->|低位为模块号, 高位为体内地址| LOW["低位交叉编址 (横向轮转存储)<br/>连续地址依次散落在不同模块 (M0, M1, M2, M3)<br/>天然支持流水线分时并发！"]
\`\`\`

---

## 3. 408 核心公式与无冲突流水线条件

设系统包含 $m$ 个并行的低位交叉存储模块，每个模块存取周期为 $T$，总线传输周期为 $r$：

### 1. 无冲突流水线成立条件（408 必考不等式）
$$m \ge \frac{T}{r}$$
只有模块数 $m$ 大于等于一个存取周期内包含的总线传输周期数，当总线轮转一周回到模块 0 时，模块 0 才能恰好完成读写准备，实现无气泡连续流水！

### 2. 连续读取 $n$ 个字的总耗时公式
$$t = T + (n - 1) \times r$$
- 第 1 个字需要经过完整的模块存取周期 $T$；
- 从第 2 个字开始，每个字只需等待一个总线周期 $r$ 即可相继流出！

### 3. 加速比推导
$$S = \frac{t_{\text{seq}}}{t} = \frac{n \times T}{T + (n - 1) \times r} \\\\\\\\xightarrow{n \to \infty} \frac{T}{r} = m$$

---

## 4. 本章小结

- 低位交叉是主存流水线的硬件根基；
- 只要满足 $m \ge T/r$，连续访存可获得接近 $m$ 倍的物理带宽提升。
`
  }
];
