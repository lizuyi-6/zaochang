// 第六部分 · 指令: 程序员与硬件的契约 (23 ~ 25)
export const part6Docs = [
  {
    id: "doc:hello-computer-part-6",
    slug: "part-6",
    parentId: "'doc:book-hello-computer'",
    title: "第六部分 · 指令: 程序员与硬件的契约",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 7,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: ""
  },
  {
    id: "doc:hello-computer-23-instruction-format",
    slug: "23-instruction-format",
    parentId: "'doc:hello-computer-part-6'",
    title: "23 指令格式与扩展操作码技术",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 23,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 23 指令格式与扩展操作码技术

## 1. 扩展操作码的核心数学本质

指令长度是固定的（例如 16 位）。
- 三地址指令需要 3 个 4 位地址码，剩下的操作码只有 4 位（最多表示 16 条指令）；
- 如果操作码被 16 种全占满，就无法再定义任何二地址或单地址指令！

**扩展操作码原则（类似哈夫曼前缀编码）**：
- **短操作码绝不能作为长操作码的前缀！**
- 必须在三地址指令中保留至少一个编码（例如 \`1111\`）作为通往下一级的“转义前缀”。

\`\`\`mermaid
flowchart TD
    ALL_16["4 位操作码 (共 16 种状态)"]
    ALL_16 --> TRIPLE["分配 15 种给【三地址指令】: 0000 ~ 1110"]
    ALL_16 --> ESCAPE["保留 1 种状态作为扩展前缀: 1111"]
    
    ESCAPE --> DBL["结合 A1 字段展开为 8 位操作码<br/>1111 0000 ~ 1111 1110 -> 分配 15 种给【二地址指令】"]
    ESCAPE --> ESC2["保留 1111 1111 作为下一级扩展前缀..."]
\`\`\`

---

## 2. 408 核心不等式

设三地址指令数为 $K_3$，二地址指令数为 $K_2$，单地址指令数为 $K_1$，零地址指令数为 $K_0$（每段地址字段 4 位）：
- $K_3 \le 2^4 - 1 = 15$；
- $K_2 \le (2^4 - K_3) \times 2^4 - 1$；
- $K_1 \le [(2^4 - K_3) \times 2^4 - K_2] \times 2^4 - 1$。

---

## 3. 本章小结

- 扩展操作码是前缀码的应用；
- 操作码长度可变，但指令总字长保持定长。
`
  },
  {
    id: "doc:hello-computer-24-addressing-modes",
    slug: "24-addressing-modes",
    parentId: "'doc:hello-computer-part-6'",
    title: "24 寻址方式大满贯: 寻址特征与有效地址 EA 计算",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 24,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 24 寻址方式大满贯: 寻址特征与有效地址 EA 计算

## 1. 408 常用 10 种寻址方式速查大表

| 寻址方式 | 有效地址 EA 计算公式 | 访存次数 (取指后) | 核心应用场景与硬件特点 |
| :--- | :--- | :--- | :--- |
| **立即寻址** | $EA = \text{形式地址 } A$ 即操作数本身 | **0 次** | 快速赋常数初值 |
| **直接寻址** | $EA = A$ | **1 次** | 全局静态变量 |
| **间接寻址** | $EA = (A)$ 或 $EA = ((A))$ | **2 次或多次** | C 语言多级指针追溯 |
| **寄存器寻址** | 操作数存放在寄存器 $R_i$ | **0 次** | 速度最快，寄存器运算 |
| **寄存器间接寻址** | $EA = (R_i)$ | **1 次** | 用寄存器存指针访存 |
| **基址寻址** | $EA = (BR) + A$ | **1 次** | **操作系统多道程序重定位**（$BR$ 对用户透明） |
| **变址寻址** | $EA = (IX) + A$ | **1 次** | **一维/多维数组线性遍历**（$IX$ 循环自增） |
| **相对寻址** | $EA = (PC) + A$ | **1 次** | **代码段相对跳转与循环**（支持浮动代码） |
| **堆栈寻址** | $EA = SP$ 隐式寻址 | **1 次** | 函数调用上下文压栈与返回 |

---

## 2. 408 世纪大陷阱：相对寻址的 $(PC)$ 预加偏移

- **陷阱**：在计算 $EA = (PC) + A$ 时，考题中的 $(PC)$ **并不是当前这条跳转指令本身的地址，而是“取出当前指令后自增更新后的 PC 地址”**！
- 若指令字长为 2 字节，取指后 $PC = PC + 2$；
- 若指令字长为 4 字节，取指后 $PC = PC + 4$。

---

## 3. 本章小结

- 寻址特征决定了硬件从形式地址 $A$ 解算有效地址 $EA$ 的算法；
- 相对寻址必须时刻警惕 PC 自增陷阱。
`
  },
  {
    id: "doc:hello-computer-25-cisc-risc",
    slug: "25-cisc-risc",
    parentId: "'doc:hello-computer-part-6'",
    title: "25 CISC 与 RISC 的哲学分歧与 Load/Store 架构",
    visibility: "public",
    authorEmail: "2251213429@qq.com",
    sortOrder: 25,
    isBook: 0,
    coverHue: 160,
    summary: "",
    bodyMd: `# 25 CISC 与 RISC 的哲学分歧与 Load/Store 架构

## 1. CISC 与 RISC 体系结构全方位对比

| 对比维度 | 复杂指令集 (CISC - 如 x86) | 精简指令集 (RISC - 如 ARM / RISC-V) |
| :--- | :--- | :--- |
| **指令系统哲学** | 丰富、复杂，一条指令能做很多事 | 极简、规整，只保留使用频率最高的基础指令 |
| **指令长度** | **变长**（1 字节 ~ 15 字节不等） | **严格定长**（通常固定为 32 位） |
| **寻址方式** | 丰富多样（直接、间接、基址变址等） | 极少，高度简化 |
| **访存指令约束** | 任意算术指令均可直接带访存操作数 | **Load/Store 架构**：只有专用的 Load 和 Store 能访存，其余一切运算只能在寄存器间进行！ |
| **通用寄存器数量** | 较少（如经典 x86 仅 8 个 GPR） | **非常多**（通常 32 个 ~ 128 个 GPR） |
| **控制器实现** | **微程序控制器为主** | **纯硬布线控制器（组合逻辑门电路网络）** |
| **流水线支持** | 很难做到高效规整流水 | **天然极其适合 5 级时钟流水线！** |

---

## 2. 本章小结

- RISC 采用定长指令和 Load/Store 架构，是现代流水线处理器的基石。
`
  }
];
